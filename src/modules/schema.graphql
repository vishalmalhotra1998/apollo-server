type Query {
  getMyProfile: User!
  getAllTrainee(data: GetAllTrainee): [Trainee]!
  getMe: User!
}

type Mutation {
  createTrainee(
    name: String!
    email: String!
    password: String!
  ): CreateTrainee!
  updateTrainee(id: ID!, update: UpdateTrainee!): ID!
  deleteTrainee(id: ID): ID!
  loginUser(payload: InputCredentials): String!
}

type Subscription {
  traineeAdded: Trainee!
  traineeUpdated: Trainee!
  traineeDeleted: ID!
}
